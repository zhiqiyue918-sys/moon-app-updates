# App Sol release packages

Published Windows installers live in this directory. `../latest.json` points to the newest stable package only after that package is fully uploaded and its SHA-256 has been recorded.
